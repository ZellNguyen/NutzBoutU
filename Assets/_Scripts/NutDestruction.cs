using UnityEngine;
using System.Collections;

public class NutDestruction : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Destroy (gameObject, 1.2f);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
